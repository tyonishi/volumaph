<UserControl x:Class="VoluMaph.UI.Controls.ColorLegendControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="120"
             d:DesignWidth="300">
    <Border Style="{StaticResource CardStyle}"
            Padding="12"
            Background="{DynamicResource CardBackground}"
            CornerRadius="8">
        <StackPanel>
            <TextBlock Text="Color Legend"
                       Style="{StaticResource Header3TextStyle}"
                       Margin="0,0,0,8"/>
            <ItemsControl ItemsSource="{Binding LegendItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5"
                                   Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="2">
                            <Border Width="40"
                                    Height="20"
                                    CornerRadius="2"
                                    Background="{Binding Color, Converter={StaticResource ColorToBrushConverter}}"
                                    BorderBrush="{DynamicResource BorderColor}"
                                    BorderThickness="1"/>
                            <TextBlock Text="{Binding Label}"
                                       FontSize="9"
                                       TextAlignment="Center"
                                       Foreground="{DynamicResource TextSecondary}"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="{Binding ThemeDescription}"
                       FontSize="10"
                       Foreground="{DynamicResource TextSecondary}"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
